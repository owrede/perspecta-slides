---
title: Column Layout Test
content-mode: advanced-slides
---

layout: 1-column

## 1-column Layout 

### Column 1

- First bullet point with longer text so that it becomes clear how wide the column is
- Second bullet point with longer text so that it becomes clear how wide the column is
- Third bullet point with longer text so that it becomes clear how wide the column is

### Column 2

- Another list with longer text so that it becomes clear how wide the column is
- In column two with longer text so that it becomes clear how wide the column is
- With more items with longer text so that it becomes clear how wide the column is

---

layout: 2-columns

## 2-column Layout 

### Column 1

- First bullet point with longer text so that it becomes clear how wide the column is
- Second bullet point with longer text so that it becomes clear how wide the column is
- Third bullet point with longer text so that it becomes clear how wide the column is

### Column 2

- Another list with longer text so that it becomes clear how wide the column is
- In column two with longer text so that it becomes clear how wide the column is
- With more items with longer text so that it becomes clear how wide the column is
